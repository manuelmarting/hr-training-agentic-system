"""KG-authoring studio (PRD §8): SOP upload → LLM extraction → human review → approve.

Nothing here self-materializes. A draft becomes a runtime graph only through an
explicit, server-validated Approve — the fail-closed posture of the PII gate applied
to graph authorship.
"""
